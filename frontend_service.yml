apiVersion: v1
kind: Service
metadata:
 name: frontend_angular
spec:
 type: NodePort
 selector:
   app: frontend_angular
   env: prod
 ports:
 - name: test
   protocol: TCP
   port: 80
   targetPort: 4200
   nodePort: 32222
